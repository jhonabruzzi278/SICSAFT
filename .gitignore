# Herramientas / IA
.claude/
.codex/
*.local.md

# Dependencias
node_modules/
vendor/
.venv/
venv/
__pycache__/
*.pyc

# Builds
dist/
build/
out/
*.tsbuildinfo
coverage/

# Tests
playwright-report/
test-results/

# Secretos y configuración local — NUNCA commitear
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
*.pfx
*.cer
# Binario compilado del instalador (iscc sicsaft-onprem.iss) — se genera localmente, no se
# versiona (mismo criterio que cualquier artefacto de build).
devops/onprem/installer/output/
# Generado por Set-DominioTraefik (instalar-cliente.ps1) a partir de dynamic.yml.template con el
# dominio real de CADA cliente — encontrado commiteado por error con el dominio de una instalación
# de prueba anterior (ADR-004 Fase 3, 2026-08-26); nunca debió versionarse, mismo criterio que
# .env (config real por instalación, no código fuente).
devops/onprem/traefik/dynamic.yml
id_rsa*
*secret*
*credential*
*service-account*.json
appsettings.*.json
!appsettings.json
!appsettings.example.json

# Vercel / despliegues
.vercel/

# Editores / SO
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*


.vercel
devops/local/docker-compose.override.yml

# Informes/entregables generados para el usuario — no son código ni documentación del repo
/SICSAFT-informe-integral-proyecto*.docx
~$*.docx
