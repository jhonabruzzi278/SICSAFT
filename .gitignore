# Herramientas / IA
.claude/
.codex/
*.local.md

# Dependencias
node_modules/
vendor/
.venv/
venv/
__pycache__/
*.pyc

# Builds
dist/
build/
out/
*.tsbuildinfo
coverage/

# Tests
playwright-report/
test-results/

# Secretos y configuración local — NUNCA commitear
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
*.pfx
*.cer
# PAT de Zitadel auto-provisionado por instalar-cliente.ps1 (devops/onprem/), secreto real por
# instalación de cliente — ver ZITADEL_FIRSTINSTANCE_PATPATH en devops/onprem/docker-compose.yml.
devops/onprem/.bootstrap/
# Binario compilado del instalador (iscc sicsaft-onprem.iss) — se genera localmente, no se
# versiona (mismo criterio que cualquier artefacto de build).
devops/onprem/installer/output/
id_rsa*
*secret*
*credential*
*service-account*.json
appsettings.*.json
!appsettings.json
!appsettings.example.json

# Vercel / despliegues
.vercel/

# Editores / SO
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*


.vercel
devops/local/docker-compose.override.yml

# Informes/entregables generados para el usuario — no son código ni documentación del repo
/SICSAFT-informe-integral-proyecto*.docx
~$*.docx
